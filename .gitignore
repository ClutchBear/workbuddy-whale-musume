# 构建产物
/target/

# dist 是运行目录：exe / 立绘副本（由根 assets/ 复制）/ 存档 / 日志，
# 以及手动复现打包流程时加入的许可声明与使用说明 —— 整体不入库
/dist/

# CI 打出的 zip（本地手动复现 workflow 时落在仓库根）
/*.zip

# 自测与一次性调试脚本产物
/_tmp/
/_old/
/_selftest/workbuddy-pet.exe
/_selftest/workbuddy-pet.log
/_selftest/fake.db

# ⚠️ tools/app.res 必须入库（build.rs 要链接它）
#    它是 app.rc 经 rc.exe 编译的二进制资源，入库后无 Windows SDK 的环境也能构建，
#    否则全新 clone / CI 会因 LNK1181 直接失败。改了 app.rc 记得重新生成它。

# 系统杂项
Thumbs.db
.DS_Store
